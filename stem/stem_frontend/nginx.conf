server {
    listen 80;
    
    # This is where your built Vite files live inside the container
    root /usr/share/nginx/html; 
    index index.html;

    location / {
        # Fall back to index.html if the route isn't found
        try_files $uri $uri/ /index.html;
    }
}